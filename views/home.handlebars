<h1>URL Shortener</h1>
<form action="/new" method="post">
  <input type="url" name="originalUrl" id="originalUrl">
  <button type="submit">SHORTEN!!!</button>
</form>
<div id="display-shortURL">
  <a
    href="{{{hostUrl}}}/{{{exitShortId}}}{{{newShortId}}}">{{{hostUrl}}}/{{{exitShortId}}}{{{newShortId}}}</a>
</div>